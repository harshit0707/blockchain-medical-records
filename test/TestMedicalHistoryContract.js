const contract = artifacts.require("MedicalHistory");

contract("", async accounts => {
  // it("should deploy the contract", async () => {
  //   let instance = await contract.deployed();
  //   assert.exists(instance, 'instance is neither `null` nor `undefined`');
  // });

  // it("should return zero medical data", async () => {
  //   let instance = await contract.deployed();
  //   let length = await instance.getMedicalDataHistoryLength();
  //   assert.equal(length, 0, 'return no medical data');
  // });
});